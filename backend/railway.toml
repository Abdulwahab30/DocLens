[build]
builder = "nixpacks"

[deploy]
startCommand = "celery -A app.worker worker --loglevel=info --pool=solo & uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/health"
healthcheckTimeout = 600
restartPolicyType = "on_failure"
