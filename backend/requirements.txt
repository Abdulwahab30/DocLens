# Python dependencies for Doc Lens
#
# These are the same packages managed by uv (pyproject.toml).
# Use this file if you prefer plain pip over uv:
#
#   python -m venv .venv
#   .venv\Scripts\activate          # Windows
#   source .venv/bin/activate       # Linux / macOS
#   pip install -r requirements.txt
#
# Minimum Python version: 3.11
#
# You still need to install and run these services separately
# (or use Docker Compose which handles them automatically):
#   - PostgreSQL  >= 14
#   - Redis       >= 6
#   - MinIO       (any recent release)
#   - Qdrant      >= 1.7
#
# See the README for non-Docker setup instructions.

# --- Web framework & server ---
fastapi>=0.136.3
uvicorn[standard]>=0.49.0          # standard = includes websockets, httptools

# --- Authentication ---
fastapi-users[sqlalchemy]>=15.0.5  # JWT login, registration, current_user dep

# --- Database ---
sqlalchemy[asyncio]>=2.0.50        # ORM + async support
asyncpg>=0.31.0                    # async PostgreSQL driver
alembic>=1.18.4                    # schema migrations

# --- Async HTTP client (used for OpenRouter API calls) ---
httpx>=0.28.1

# --- Settings / config ---
pydantic-settings>=2.14.1          # reads .env into typed Settings class
python-dotenv>=1.2.2               # .env file loading

# --- Object storage (MinIO / AWS S3) ---
boto3>=1.43.24                     # S3-compatible client

# --- Task queue ---
celery>=5.6.3                      # distributed task queue
redis>=8.0.0                       # Python Redis client (Celery broker backend)

# --- Vector database ---
qdrant-client>=1.18.0              # Qdrant Python SDK

# --- PDF parsing ---
docling>=2.98.0                    # Docling PDF parser (text, tables, figures)
                                   # NOTE: Docling downloads additional ML models
                                   # (~1-2 GB) on first use from HuggingFace.
                                   # Requires internet access on first run.

# --- Embeddings & reranking ---
sentence-transformers>=5.5.1       # Loads two models automatically on first use:
                                   #   BAAI/bge-small-en-v1.5  (~130 MB) — embeddings
                                   #   ms-marco-MiniLM-L-6-v2  (~85 MB)  — cross-encoder reranker
